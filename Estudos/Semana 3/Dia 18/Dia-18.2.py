'''
🟡 18.2 – Notas e estatísticas (intermediário)

Crie um array 2D (matriz) representando as notas de 4 alunos em 3 provas, por exemplo:

notas = np.array([
    [7.5, 8.0, 6.5],
    [5.0, 6.0, 7.0],
    [9.0, 8.5, 9.5],
    [4.0, 5.5, 6.0]
])


Seu programa deve:

Calcular a média de cada aluno (por linha).
Calcular a média de cada prova (por coluna).
Descobrir a maior nota geral e sua posição (índice do aluno e da prova).

Requisitos:

Usar axis=0 e axis=1 nas funções de NumPy.
Usar np.argmax para localizar o índice da maior nota.
'''